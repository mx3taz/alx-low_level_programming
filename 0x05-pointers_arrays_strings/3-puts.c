#include "main.h"

/**
 * _puts - Prints a string
 *
 * @str: Pointer to the string
 */
void _puts(char *str)
{
	int i;

	for (i = 0; *s != '\0'; s++)
	{
		_putchar(s[i]);
		i++;
	}
	_putchar('\n');
}
