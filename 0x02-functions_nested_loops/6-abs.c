#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 *
 * Return: The absolute value
 */
int _abs(int)
{
	int absoluteValue;

	if (absoluteValue < 0)
		return (absoluteValue * -1);
}
